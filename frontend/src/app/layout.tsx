import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"], display: "swap" });

export const metadata: Metadata = {
  title: "NaijaDrive - Nigeria's Premier Car Marketplace",
  description: "Find your perfect drive in Nigeria. Browse thousands of verified cars from trusted dealers across Lagos, Abuja, Port Harcourt, and more.",
  keywords: "cars, Nigeria, buy cars, sell cars, tokunbo, Lagos, Abuja, car marketplace",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" suppressHydrationWarning>
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
      </head>
      <body className={inter.className}>{children}</body>
    </html>
  );
}
